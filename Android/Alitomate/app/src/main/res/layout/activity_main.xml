<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <android.widget.Button
        android:id="@+id/report_button"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:text="Lapor Kebakaran"
        android:textColor="@color/white"
        android:textSize="35sp"
        android:background="@drawable/custom_circle_button"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <android.widget.Button
        android:id="@+id/emergency_button"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_margin="32dp"
        android:text="Panggilan Darurat"
        android:textSize="16sp"
        android:textColor="@color/white"
        android:background="@drawable/custom_w_stroke_button"
        app:layout_constraintEnd_toEndOf="@+id/report_button"
        app:layout_constraintStart_toStartOf="@+id/report_button"
        app:layout_constraintTop_toBottomOf="@+id/report_button" />

</androidx.constraintlayout.widget.ConstraintLayout>